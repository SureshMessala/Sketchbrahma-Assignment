<div class="container">
  <div class="row">
    <div id="side-menu"  class="col-sm-3">
    </div><!-- side-menu -->
    <div id="main-content" class="col-sm-9">
       <h1>Tasks</h1>
      <%= render "task", tasks: @tasks %>
    </div>
    <%= link_to "Add new task", new_task_path, :class => "navbar-brand" %>
    <%= link_to "Back", root_path, :class => "navbar-brand" %>
    <%= link_to 'Remove', project_path(@project.id), method: 'delete', data: { confirm: 'Are you sure?' } %>
  </div>
</div>
